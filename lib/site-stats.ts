/**
 * Marketing / UI catalog size — use this in visible copy and meta instead of raw `games.json` length
 * so the public site stays consistent (e.g. 1000+ vs exact file counts).
 */
export const PUBLIC_CATALOG_SIZE_LABEL = "1000+";
